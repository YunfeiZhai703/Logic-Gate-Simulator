devices [
	G1, G2 = NAND;     # defaults to 2 inputs
	SW1, SW2 = SWITCH; # defaults to 0 i.e "False"
]

conns [
	SW1 = G1.1;
	SW2 = G2.2;
	G1=G2.1;           # Whitespace not required
	G2 = G2.2;
]

# new blank lines are ignored

monit [
	G1, G2;
]
